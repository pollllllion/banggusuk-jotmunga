-- ============================================================
-- 큐레이션 맺음말(outro) 칸 (2026-09-17)
-- ============================================================
-- 화면 순서는 '본문 문단 → 작품 카드' 뿐이라, "하나만 고른다면?" 처럼 작품을 다 소개한
-- 뒤에 와야 하는 글을 놓을 자리가 없었다. 작품 카드 아래에 그리는 칸을 따로 둔다.
--
-- 추가형이다 — 배포보다 먼저 실행할 것. 새 코드는 상세 로드에서 이 칸을 select 하므로
-- 컬럼이 없으면 큐레이션 상세가 본문을 못 불러온다. 옛 코드는 이 칸을 안 읽어서 미리 넣어도 무해.
--
-- 멱등이다. 여러 번 실행해도 안전. Supabase SQL Editor 에 통째로 붙여 실행.
-- 실행 후: npm run migrate:mark migration_curation_outro.sql
-- ============================================================

alter table public.curations
  add column if not exists outro text not null default '';
