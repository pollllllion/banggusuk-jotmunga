node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.vite

# 빌드 때 DB에서 자동 생성 (scripts/generate-sitemap.mjs)
public/sitemap.xml

# 작품 병합 백업 (유저 리뷰 원문 포함 — public repo 에 올리지 않는다)
scripts/merge-backup-*.json

# 시드 작품 삭제 백업 (일회성 정리 기록)
scripts/seed-contents-backup.json

# 시드 페르소나 계정(비밀번호 포함)·게시 큐 — public repo 에 올리지 않는다
scripts/personas.local.json
scripts/queue.json
